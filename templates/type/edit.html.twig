{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
    <h1>Edit Type</h1>

    {{ include('type/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm my-2" href="{{ path('type_index') }}">Volver atrás</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
